# Windows-Threat-Monitoring-project
Windows blue-team project using Sysmon and PowerShell to detect suspicious activity such as encoded PowerShell commands, persistence via registry Run keys, failed logons, suspicious processes, and abnormal network connections. Built for hands-on SOC experience.
